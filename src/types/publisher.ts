export interface PublisherSearchForm {
  publisherName: string
}

export interface PublisherDialogForm {
  id: number | string
  publisherName: string
}

export interface TablePublisherData {
  id: number
  publisherName: string
}
